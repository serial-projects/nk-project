/**
 * Ruka Raposa Project by Ruka Raposa Team & Contribuitors.
 * Project is under GPL3-license, read LICENSE.md on the root.
 */
#ifndef NKEnumsTaskStateMachine_h 
#define NKEnumsTaskStateMachine_h 

/**
 * @brief Defines the number for the `NK_StateMachine` for when the machine is
 * `RUNNING`
 */
#define NK_ENUMS_STATE_MACHINE_RUNNING                                          0

/**
 * @brief Defines the number for the `NK_StateMachine` for when the machine is
 * `FINISHED`
 */
#define NK_ENUMS_STATE_MACHINE_FINISHED                                         1

/**
 * @brief Defines the number for the `NK_StateMachine` for when the machine is
 * `DIED`
 */
#define NK_ENUMS_STATE_MACHINE_DIED                                             2

/**
 * @brief Defines the number for the `NK_StateMachine` for when the machine is
 * `WAITING`
 */
#define NK_ENUMS_STATE_MACHINE_WAITING                                          3

#endif